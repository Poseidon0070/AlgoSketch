version: '3.8'

services:
  nextjs_app:
    build: .
    container_name: algosketch
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    restart: always
